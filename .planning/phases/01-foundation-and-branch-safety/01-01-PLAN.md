---
phase: 01-foundation-and-branch-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04
must_haves:
  truths:
    - "Running `git branch -a` shows main, dev, demo, backend, frontend, and mixed branches"
    - "All 6 branches exist on the remote (origin)"
    - "`.planning/STATE.md` and `.planning/phases/*/*-CONTEXT.md` are listed in .gitignore"
    - "Shared .planning/ files (ARCHITECTURE.md, REQUIREMENTS.md) are still tracked by git"
  artifacts:
    - path: ".gitignore"
      provides: "Session-specific .planning/ file exclusions"
      contains: ".planning/STATE.md"
  key_links:
    - from: "local dev branch"
      to: "origin/dev"
      via: "git push origin dev"
      pattern: "branch.*dev"
    - from: ".gitignore"
      to: ".planning/STATE.md"
      via: "gitignore exclusion rule"
      pattern: "\\.planning/STATE\\.md"
---

<objective>
Establish the 6-branch Git strategy and protect .planning/ session files from merge conflicts.

Purpose: Team members (backend, frontend owners) need independent branches before any code changes begin. The branch strategy (D-09 through D-14) is the prerequisite for all parallel Phase 2+ work.

Output: Six long-lived branches (main, dev, demo, backend, frontend, mixed) pushed to origin, and .gitignore updated so session-specific .planning/ files do not cause merge conflicts between team members using Claude Code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-branch-safety/01-CONTEXT.md

<!-- Key decisions honored by this plan:
  D-09: 6 long-lived branches: main, dev, demo, backend, frontend, mixed
  D-10: Merge flow: frontend + backend -> mixed -> dev -> main
  D-11: Demo = fork from dev at stable point, freeze 48h before jury
  D-12: No branch protection rules (team trusts each other)
  D-13: All branches are long-lived (persist throughout the project)
  D-14: .planning/ session files need gitignore rules to avoid Claude Code conflicts
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and push the 6 long-lived branches</name>
  <files>.git (branch refs — no source file modified)</files>
  <read_first>
    - Run `git branch -a` to see which branches already exist before creating any
    - Run `git status` to confirm the working tree is clean before branching
  </read_first>
  <action>
From the current `main` branch (already exists), create the five new branches in this exact order, then push all of them to origin in one command.

Per D-09 (locked): branches are `dev`, `demo`, `backend`, `frontend`, `mixed`.
Per D-12 (locked): NO branch protection rules. These are plain branches, no GitHub ruleset needed.
Per D-13 (locked): All branches are long-lived — do not set any expiry or delete-after-merge policies.

Commands to execute:
```bash
git checkout main
git checkout -b dev
git checkout -b demo
git checkout main
git checkout -b mixed
git checkout -b backend
git checkout -b frontend
git checkout main
git push origin dev demo mixed backend frontend
```

After push, run `git branch -a` to confirm all 6 are present locally and remotely.

Branch ownership memo (do NOT encode in git, just note for team):
- `main`: production-stable
- `dev`: integration canary
- `demo`: jury presentation (freeze T-48h per D-11)
- `mixed`: frontend + backend integration before promoting to dev
- `backend`: owned by this user (backend developer)
- `frontend`: owned by teammate (frontend developer)

Merge flow per D-10: frontend + backend -> mixed -> dev -> main
  </action>
  <verify>
    <automated>git branch -a | grep -E "dev|demo|mixed|backend|frontend" | wc -l</automated>
    Expected output: 10 (5 local + 5 remote refs)
  </verify>
  <acceptance_criteria>
    - `git branch -a` output contains: `dev`, `demo`, `mixed`, `backend`, `frontend` (local)
    - `git branch -a` output contains: `remotes/origin/dev`, `remotes/origin/demo`, `remotes/origin/mixed`, `remotes/origin/backend`, `remotes/origin/frontend`
    - Current branch after task completes is `main`
  </acceptance_criteria>
  <done>All 6 branches exist locally and on origin; working branch is main</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore with session-specific .planning/ exclusions</name>
  <files>.gitignore</files>
  <read_first>
    - Read `.gitignore` in full before editing — it already has many entries, add to the end without disrupting existing rules
  </read_first>
  <action>
Per D-14 (locked): Multiple team members use Claude Code simultaneously. Two files conflict because each session writes different per-session state:
- `.planning/STATE.md` — tracks per-session position (which plan the user is on); two members at different positions will always conflict
- `.planning/phases/*/*-CONTEXT.md` — generated per discussion session, not consumed by running app

Files to KEEP tracked (shared knowledge — do not exclude):
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/PROJECT.md`
- `.planning/codebase/*.md`
- `.planning/phases/*/` PLAN.md, SUMMARY.md, RESEARCH.md, VALIDATION.md files
- `.planning/research/`

Add the following block at the END of `.gitignore` (after existing content — do NOT replace existing content):

```gitignore
# GSD planning session files (per-session state — conflict-prone between team members)
# Shared planning artifacts (ROADMAP, REQUIREMENTS, PLAN files) remain tracked.
.planning/STATE.md
.planning/phases/*/*-CONTEXT.md
.planning/phases/*/*-DISCUSSION-LOG.md
```

Do not remove any existing lines from .gitignore. Append only.

After editing, run `git status` to confirm:
- `.planning/STATE.md` now shows as ignored (if it was tracked before, it may still appear as tracked — that is expected; only newly created files will be ignored automatically)
- Run `git check-ignore -v .planning/STATE.md` to confirm the rule fires
  </action>
  <verify>
    <automated>git check-ignore -v ".planning/STATE.md"</automated>
    Expected: output contains `.planning/STATE.md` (the rule matched)
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the line `.planning/STATE.md`
    - `.gitignore` contains the line `.planning/phases/*/*-CONTEXT.md`
    - `.gitignore` contains the line `.planning/phases/*/*-DISCUSSION-LOG.md`
    - `git check-ignore -v .planning/STATE.md` exits 0 and prints a matching rule
    - All previously tracked files are still tracked (`git status` shows no unexpected deletions)
  </acceptance_criteria>
  <done>.gitignore excludes session-specific .planning/ files; shared planning artifacts remain tracked</done>
</task>

</tasks>

<verification>
After both tasks:
1. `git branch -a` shows main, dev, demo, mixed, backend, frontend (local and remote)
2. `git check-ignore -v .planning/STATE.md` returns a match
3. `git check-ignore -v .planning/ROADMAP.md` returns no match (shared file, should remain tracked)
4. Working branch is `main`
</verification>

<success_criteria>
Per FOUND-03: Git branching strategy with all 6 branches established and pushed to origin.
Per FOUND-04: Demo freeze protocol is team-agreement-based (T-48h per D-11); no branch protection rules per D-12. .gitignore protection prevents .planning/ conflicts for multiple Claude Code users.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-branch-safety/01-01-SUMMARY.md`
</output>
